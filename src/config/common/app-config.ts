import type { IAppConfig } from './interface';

export const AppConfig: IAppConfig = {
  appName: 'VoGen',
  appDescription: 'Make it your own invoice generator and download it then.',
  appDonate: 'https://saweria.co/imaha7',
};
